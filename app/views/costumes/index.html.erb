<h1>All Costumes</h1>
<ul>
  <% @costumes.each do |costume| %>
  <li>
  <%= link_to costume.name, costume_path(costume.id) %>
 </li>
 <% end %>
</ul>
<%= link_to "Add a new Costume", new_costume_path(@costume)%>
